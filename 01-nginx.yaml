- name: installing nginx
  hosts: web
  become: yes

  tasks:
    - name: install nginx
      ansible.builtin.package:
        state: installed
      
    - name: start and enable the nginx
      ansible.builtin.service:
        state: restarted
        enabled: yes  
